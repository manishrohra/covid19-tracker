export interface GlobalDataSummary{
//    total cases of each country
//    ? is for not a mandatory field
country ?: string , 
confirmed ?: number , 
deaths ?: number, 
recovered ?:number , 
active ?: number 
}